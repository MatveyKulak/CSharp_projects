<Window x:Class="Hospital.WPF.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Вход в систему" SizeToContent="WidthAndHeight" WindowStartupLocation="CenterScreen"
        WindowStyle="ToolWindow" ResizeMode="NoResize">
    <Border Padding="20" MinWidth="300">
        <StackPanel>
            <TextBlock Text="Логин:" FontWeight="SemiBold"/>
            <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,15" FontSize="14"/>

            <TextBlock Text="Пароль:" FontWeight="SemiBold"/>
            <PasswordBox x:Name="PasswordInput" Margin="0,5,0,20" FontSize="14"/>

            <Button Content="Войти" IsDefault="True" Click="LoginButton_Click" Padding="0,8" FontSize="14" FontWeight="Bold"/>
        </StackPanel>
    </Border>
</Window>